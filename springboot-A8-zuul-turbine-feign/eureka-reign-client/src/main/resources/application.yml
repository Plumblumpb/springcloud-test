spring:
  application:
    name: eureka-feign-client
server:
  port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

# feign自带hystrix ，开启該功能即可。
feign:
  hystrix:
    enabled: true
