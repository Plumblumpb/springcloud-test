spring:
  cloud:
    config:
      server:
        native:
#          配置文件位置
          search-locations: classpath:/shared
  profiles:
    active: native
  application:
    name: config-server

server:
  port: 8769